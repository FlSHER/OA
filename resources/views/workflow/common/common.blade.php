<!--common-->
<!--<style>
    .header_data>div{float: left;}
    .nav_head{position:relative;width:130px;}
    .flow_set{width:130px;color: #000000; height:40px; line-height:40px; text-align: center;font-size:18px;font-weight:bold;}
    .form_dataAll{display: none;position:absolute;border: 1px solid #ccc;z-index:2;width: 130px;background:#fff;}
    .form_dataAll a{display: block;color:#333;height:30px;text-decoration: none;line-height: 30px;padding-left: 10px;}
    .form_dataAll a:hover{background: #008000;color:#fff;}
    .nav_head:hover .form_dataAll{display:block;}
    .nav_head:hover .flow_set{background: #1687cb;color:#fff;}

    .flow_set a:hover{background: #1687cb;color:#fff;}
    .flow_set a{display: block;color:#000000;height:40px;text-decoration: none;line-height: 40px;font-size:18px;}
</style>
<div class="collapse navbar-collapse header_data" >
    <div class="flow_set"><a href="{{asset(route('workflow.home'))}}">运行流程</a></div>
    <div class="flow_set"><a href="{{asset(route('workflow.releaseFlow'))}}">已发布流程</a></div>
    <div class="nav_head">
        <div class="flow_set">工作流程设置<b class="caret"></b></div>
        <div class="form_dataAll">
            <a href="{{asset(route('workflow.formConfigList'))}}">表单设置</a>
            <a href="{{asset(route('workflow.flowConfigList'))}}">工作流设置</a>
            <a href="#">报表设置</a>
            <a href="{{asset(route('workflow.classify'))}}">分类设置</a>
        </div>
    </div>
    <div class="flow_set"><a href="{{asset(route('workflow.flowTypeList'))}}">流程管理</a></div>
    <div class="flow_set"><a href="#">流程日志</a></div>
    <div class="nav_head">
        <div class="flow_set">管理设置<b class="caret"></b></div>
        <div class="form_dataAll">
            <a href="{{asset(route('workflow.databaseManage'))}}">数据源管理</a>
        </div>
    </div>
</div>-->
